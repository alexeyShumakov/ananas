<% if user_signed_in? && policy(post).update? %>
    <%= link_to 'delete', post_path(post.id), method: :delete, data: { confirm: 'delete this post and comments?' } %>
    or <%= link_to 'edit', edit_post_path(post.id) %>
<% end %>
