<div class="panel panel-default devise-form">
  <div class="panel-body">
    <%= simple_form_for(resource, as: resource_name, url: password_path(resource_name),
                        html: { method: :post, class: 'form-horizontal' }, wrapper: :horizontal_form) do |f| %>
      <h2 class='text-center'>Забыли пароль?</h2>
      <%= f.error_notification %>
      <%= f.full_error :confirmation_token %>

      <div class="form-inputs">
        <%= f.input :email, required: true, autofocus: true %>
      </div>

      <div class="form-group">
        <div class="col-md-offset-3 col-md-9">
          <%= f.button :submit, "Выслать инструкцию по восстановлению пароля" %>
        </div>
      </div>
      <div class="form-group">
        <div class="col-md-offset-3 col-md-9">
          <%= render "devise/shared/links" %>
        </div>
      </div>
    <% end %>
  </div>
</div>
